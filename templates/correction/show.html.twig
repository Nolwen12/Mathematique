{% extends 'base.html.twig' %}

{% block title %}Correction{% endblock %}

{% block body %}
    <h1>Correction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ correction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ correction.name }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td><a href="{{ asset('uploads/Contenue/' ~ correction.Contenue) }}">Voir la correction</a></td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ correction.date ? correction.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_correction_index') }}">back to list</a>

    <a href="{{ path('app_correction_edit', {'id': correction.id}) }}">edit</a>

    {{ include('correction/_delete_form.html.twig') }}
{% endblock %}
